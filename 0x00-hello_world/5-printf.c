#include <stdio.h>
/**
 * main - A program that print a line usint the printf function
 *Return: 0 (success)
 */

int main(void)
{
	prinf("with proper grammar, but the outcome is a piece of art,\n")
		return (0);
}
