#include <stdio.h>

#include <stdlib.h>

#include <time.h>

/**
 * main - base 10 starting from 0
 * Return: Always 0 (success)
 * A
 */
int main(void)
{
	int t;

	for (d = '0'; d <= '9'; d++)
	       putchar(	(d);
			       putchar('\n')

			       return (0) (success)
			       }
